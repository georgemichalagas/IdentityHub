# Development Environment Variables
ASPNETCORE_ENVIRONMENT=Development

# API Configuration
API_PORT=5040

# Database Configuration
DB_HOST=localhost
DB_NAME=identityhub_dev
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# JWT Configuration
JWT_SECRET_KEY=DevelopmentSecretKeyForJWTTokensNotForProduction123456789

# Email Configuration (Optional for development)
SMTP_SERVER=smtp.ethereal.email
SMTP_PORT=587
SMTP_USERNAME=your_ethereal_username
SMTP_PASSWORD=your_ethereal_password
FROM_EMAIL=noreply@identityhub-dev.local

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://localhost:3000

# Rate Limiting
RATE_LIMIT_REQUESTS_PER_MINUTE=200
